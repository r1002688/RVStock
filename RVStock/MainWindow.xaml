<Window x:Class="RVStock.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RVStock - Stockbeheer"
        Height="550"
        Width="900">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 15">
            <TextBlock Text="Stockbeheer"
                       FontSize="24"
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>

            <TextBlock Text="Scan barcode:"
                       VerticalAlignment="Center"
                       Margin="30 0 8 0"/>

            <TextBox x:Name="BarcodeTextBox"
                     Width="220"
                     Height="32"
                     VerticalContentAlignment="Center"
                     KeyDown="BarcodeTextBox_KeyDown"/>

            <Button Content="Vernieuwen"
                    Width="100"
                    Height="32"
                    Margin="15 0 0 0"
                    Click="RefreshButton_Click"/>

            <Button Content="⚙ Beheer"
                    Width="100"
                    Height="32"
                    Margin="10 0 0 0"
                    Background="#607D8B"
                    Foreground="White"
                    FontWeight="Bold"
                    Click="BeheerButton_Click"/>
        </StackPanel>

        <DataGrid x:Name="ProductsGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  AlternatingRowBackground="#F5F5F5">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"
                                    Binding="{Binding Id}"
                                    Width="60"/>
                <DataGridTextColumn Header="Naam"
                                    Binding="{Binding Naam}"
                                    Width="*"/>
                <DataGridTextColumn Header="Barcode"
                                    Binding="{Binding Barcode}"
                                    Width="150"/>
                <DataGridTextColumn Header="Voorraad"
                                    Binding="{Binding Voorraad}"
                                    Width="100"/>
                <DataGridTextColumn Header="Leverancier"
                                    Binding="{Binding Leverancier.Naam}"
                                    Width="180"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
